{
    "project": {
        "name": "mybgg",
        "title": "Welcome to Boardom's End!"
    },
    "boardgamegeek": {
        "user_name": "ashleyspub",
        "extra_params": {
            "own": 1
        }
    },
    "algolia": {
        "app_id": "PNTS2AEN3U",
        "api_key_search_only": "61b77b17ee869a9e23ca2f7e7adadb53",
        "index_name": "PubGames",
        "hits_per_page": 24
    }
   { document.addEventListener('DOMContentLoaded', function() {
  // Find the sorting mechanism on your page
  // This assumes you have some sort of select or button elements for sorting
  
  // Option 1: If you have a select dropdown for sorting
  const sortSelect = document.querySelector('select[data-sort-by]') || document.querySelector('.sort-select');
  if (sortSelect) {
    // Set the value to 'rating' instead of 'name'
    sortSelect.value = 'rating';
    // Trigger the change event to apply the sorting
    sortSelect.dispatchEvent(new Event('change'));
  }
}
